#include <bits/stdc++.h>
using namespace std;

#define int long long

int32_t main() {
    // std::ios::sync_with_stdio(false);
    // std::cin.tie(nullptr);

    int t;
    cin >> t;
    while(t--){
        int n;
        cin >> n;
    }
}
